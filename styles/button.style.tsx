import themeStyle from "./theme.style";

import Theme from './theme.style';

export default {
    btn: {
        height: 50
    },
    btnPrimary: {
        // backgroundColor: Theme.primary,
        // fontWeight: 'bold',
    }
}